# test — ephemeral MySQL + PostgreSQL for integration tests
# No persistent volumes: all data is lost on `docker compose down`.
MYSQL_ROOT_PASSWORD=dev_root_pass
MYSQL_USER=dev_user
MYSQL_PASSWORD=dev_pass
POSTGRES_USER=dev_user
POSTGRES_PASSWORD=dev_pass
